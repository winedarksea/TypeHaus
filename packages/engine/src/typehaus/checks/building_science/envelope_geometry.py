"""Moved to :mod:`typehaus.resolve.envelope_geometry` so emitters may read it too."""

from typehaus.resolve.envelope_geometry import (  # noqa: F401
    EnvelopeGeometry,
    _face_probe_lines,
    _wall_body,
    carries_a_weather_skin,
    envelope_geometry,
)
